// =============================================================
// uploadMiddleware.js — Middleware Upload Foto Barang
// TODO: gunakan multer + config/cloudinary.js untuk menyimpan
// foto barang yang dilaporkan lewat lapor.html.
// =============================================================

// const multer = require("multer");
// const upload = multer({ dest: "uploads/" });
// module.exports = upload;

module.exports = (req, res, next) => {
  // Placeholder: lewati saja untuk sekarang
  next();
};
